renderedurl: https://s3-eu-west-1.amazonaws.com/architect/<%=node.config.cluster%>/var/rendered/<%=node.platform%>/node/<%=node.name%>

networks:
  network1:
    interface: eth0
  network2:
    interface: eth1
  network3:
    interface: eth2
